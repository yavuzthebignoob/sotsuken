#include <vector>
using namespace std;

class NTuple {
  // 他のコードでNTupleクラスが定義されていないというコンパイルエラーを出さないための
  // 内容なしのコード、まだ実装はしていない
};

class NTuples {
public:
  int getValue(vector<double> input);
};
